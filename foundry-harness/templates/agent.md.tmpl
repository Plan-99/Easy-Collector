---
name: {{AGENT_NAME}}
description: {{언제 이 agent를 부르는가 — 한 줄}}
tools: {{이 역할에 꼭 필요한 도구만}}
---

# {{AGENT_NAME}}

> 역할 하나만 맡는다. 역할이 둘이면 agent를 둘로 나눠라.
> 이 agent도 세금이다 — 실제 필요(관찰된 작업 유형)가 있어서 존재해야 한다.

## 책임
{{이 agent가 책임지는 단 하나의 일}}

## 입력 / 출력
- 입력: {{}}
- 출력: {{다음 단계가 바로 쓸 수 있는 형태}}

## 지켜야 할 것
- `golden-principles.md`를 따른다.
- {{이 역할 특유의 제약 — 기계적으로 검증 가능한 것만}}

## 하지 않는 것 (경계)
- {{다른 agent의 영역}}
