# {{PROJECT_NAME}} 사용자 매뉴얼

> 이 파일은 **두 독자**를 섬긴다: 사람 사용자, 그리고 **nightwatch**(각 시나리오를 자동으로 따라 누르며
> 버그·깨진 UI 를 찾는 루프). 그래서 모든 기능은 *실행 가능한 시나리오*로 적는다 — 추상 설명 금지.
> **갱신은 손이 아니라 `manual-writer` agent 가** 한다(기능 표면이 바뀌면 `scripts/manual-check.sh` 가 빨간불).
>
> 메타: `feature-surface:` 아래 glob 이 이 매뉴얼이 책임지는 기능 표면이다(게이트가 이 목록을 본다).

<!-- feature-surface: {{FEATURE_GLOBS}} -->

## 시작하기
- 실행: {{HOW_TO_RUN}}
- 전제: {{PREREQUISITES}}

## 기능 시나리오
> 각 `###` 블록 = nightwatch 가 1회 수행하는 시나리오. 단계·기대 결과·엣지를 *관찰 가능하게*.

### {{기능명}}
{{한 줄 목적}}

- **단계**
  1. {{사용자가 누르는/입력하는 것 — 버튼·필드·명령을 구체적으로}}
  2. ...
- **기대 결과**: {{무엇이 보여야/일어나야 하는가 — 자동 판정 가능한 관찰}}
- **엣지/실패**: {{빈 입력·최대 길이·잘못된 값일 때의 올바른 동작}}

<!-- 기능마다 ### 블록을 반복. 코드의 feature-surface 와 1:1 이어야 게이트가 통과한다. -->
