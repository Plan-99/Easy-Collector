# Pattern: fan-out / fan-in (병렬 분할/수렴)

**언제**: 서로 독립적인 작업이 다수이고, 마지막에 결과를 모을 때.

**구조**: 분배자가 N개로 쪼갬 → 병렬 실행 → 수렴자가 병합. 수렴 시 dedup/충돌 처리.

**비용(tax)**: 중간. 병렬 결과 병합 로직이 추가 부담.

**OMC 매핑**: Ultrawork(최대 병렬).

**안티패턴**: 작업 간 숨은 의존이 있는데 병렬화 → 경쟁 조건. 의존 있으면 `pipeline`.
