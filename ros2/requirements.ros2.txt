# ===================================================================
# EasyTrainer ROS2 Bridge Container - Python Dependencies
# Dockerfile.ros2에서 사용. 로봇 제어 + gRPC 브릿지에 필요한 패키지만 포함.
# PyTorch, Flask, 프론트엔드 관련 패키지는 메인 컨테이너(requirements.txt) 참조.
# ===================================================================

# --- [gRPC 브릿지] ---
grpcio
grpcio-tools
protobuf

# --- [데이터 처리] ---
numpy==1.26.4
opencv-python-headless

# --- [로봇 SDK] --- (모듈 설치 시 install.sh로 자동 설치)
# piper-sdk, dynamixel_sdk, pyserial, python-can, pymodbus, pyusb

# --- [IK solver] ---
pin-pink
proxsuite

# --- [Sim] --- (sim_mujoco_tutorial 번들 모듈; 항상 사용)
mujoco>=3.2.0
